项目由Qt 4.11.0 创建。

如果不兼容，可按如下步骤处理：
（1） 用自己机器上的Qt创建一个空的项目；
（2） 把下载项目文件目录中的所有 .h .cpp 文件(含 main.cpp) 拷贝到自己创建的项目目录；
（3） 通过“添加现有文件...” 的方式，把拷贝过去的所有类文件添加到新建项目中。


注意：
代码提交时，手动把不同文件中的代码合并，去除对本地头文件的包含，合并对系统头文件的包含。
