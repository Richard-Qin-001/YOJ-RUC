#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n, s, d;
    cin >> n >> s >> d;
    vector<vector<int>> roads(n, vector<int>(n));
    for(auto&r:roads)for(auto&c:r)cin >> c;
    
}